name: InWork Continuous Quality

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Open-source test stack:
# - Microsoft Playwright for browser E2E tests
# - Google's Android emulator container project is the reference model for
#   emulator-based Android validation.
# - reactivecircus/android-emulator-runner provides the GitHub-hosted runner
#   integration for a real Android emulator.

jobs:
  web-e2e:
    name: Web E2E (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install test dependencies
        run: npm install --no-audit --no-fund
      - name: Install Chromium
        run: npx playwright install chromium
      - name: Run browser E2E
        run: npx playwright test --config=playwright.ci.config.js
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ matrix.os }}
          path: playwright-report/
          if-no-files-found: ignore

  android-emulator:
    name: Android Emulator Smoke Test
    needs: web-e2e
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Prepare bundled web assets
        shell: bash
        run: |
          set -euo pipefail
          rm -rf android/app/src/main/assets
          mkdir -p android/app/src/main/assets/web
          test -s index.html
          cp index.html android/app/src/main/assets/web/index.html
          cp manifest.webmanifest android/app/src/main/assets/web/manifest.webmanifest
          cp sw.js android/app/src/main/assets/web/sw.js
      - name: Build APK
        working-directory: android
        run: gradle :app:assembleDebug --no-daemon
      - name: Start Android emulator and test APK
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          target: google_apis
          arch: x86_64
          profile: pixel_2
          emulator-options: -no-window -no-audio -no-boot-anim -gpu swiftshader_indirect
          disable-animations: true
          script: |
            set -e
            adb install -r android/app/build/outputs/apk/debug/app-debug.apk
            adb logcat -c
            adb shell am force-stop com.inwork.app || true
            adb shell monkey -p com.inwork.app 1
            sleep 10
            adb shell input keyevent KEYCODE_DPAD_CENTER || true
            sleep 2
            adb shell uiautomator dump /sdcard/inwork-window.xml || true
            adb pull /sdcard/inwork-window.xml inwork-window.xml || true
            adb exec-out screencap -p > inwork-emulator.png
            adb logcat -d > inwork-logcat.txt
            grep -q 'INWORK_READY.*true' inwork-logcat.txt
            test -s inwork-emulator.png
            echo 'ANDROID_SMOKE_TEST=PASS'
      - name: Upload Android smoke evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-emulator-evidence
          path: |
            inwork-emulator.png
            inwork-window.xml
            inwork-logcat.txt
            android/app/build/outputs/apk/debug/app-debug.apk
            android/app/build/outputs/apk/debug/output-metadata.json
          if-no-files-found: warn

  apk-release-candidate:
    name: APK Release Candidate
    needs: android-emulator
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Bundle web assets
        run: |
          set -euo pipefail
          rm -rf android/app/src/main/assets
          mkdir -p android/app/src/main/assets/web
          cp index.html android/app/src/main/assets/web/index.html
          cp manifest.webmanifest android/app/src/main/assets/web/manifest.webmanifest
          cp sw.js android/app/src/main/assets/web/sw.js
      - name: Build release candidate APK
        working-directory: android
        run: gradle :app:assembleDebug --no-daemon
      - name: Upload verified APK
        uses: actions/upload-artifact@v4
        with:
          name: InWork-verified-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
